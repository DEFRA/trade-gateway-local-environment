info:
  name: cheds
  type: folder
  seq: 2

request:
  auth:
    type: bearer
    token: "{{test_ched_reader_access_token}}"
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 401) {
          bru.runRequest("auth/test-ched-reader");
          console.log("401 unauthorized returned, new access token for test-ched-reader requested.");
        }
