info:
  name: single
  type: http
  seq: 3

http:
  method: GET
  url: "{{service_url}}/certificates/docoms/DOCOM.ES.2026.0000001"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
