info:
  name: test-docom-reader
  type: http
  seq: 3

http:
  method: POST
  url: "{{service_url}}/local/cognito/token"
  body:
    type: form-urlencoded
    data:
      - name: scope
        value: "{{access_token_scope}}"
      - name: sub
        value: test-docom-reader
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("test_docom_reader_access_token", res.body.access_token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
