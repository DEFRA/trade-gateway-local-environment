info:
  name: intras
  type: folder
  seq: 2

request:
  auth: inherit
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 401) {
          bru.runRequest("auth/test-intra-reader");
          console.log("401 unauthorized returned, new access_token for test-intra-reader requested.");
        }
